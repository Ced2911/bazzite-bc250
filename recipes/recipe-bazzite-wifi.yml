# yaml-language-server: $schema=https://schema.blue-build.org/recipe-v1.json
name: bazzite-wifi
description: Bazzite with rtw88 wifi driver

base-image: ghcr.io/ublue-os/bazzite-deck
image-version: stable 

modules:
  - type: script
    scripts:
      - rtw88.sh
  - type: signing
